/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */
/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */
// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: 34tvEQuyqfK98iGCjMbawB
// Component: 1R3VLUIpzgA9
import * as React from "react";
import { useRouter } from "next/router";
import {
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  useDollarState
} from "@plasmicapp/react-web";
import { useDataEnv } from "@plasmicapp/react-web/lib/host";
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: 34tvEQuyqfK98iGCjMbawB/styleTokensProvider
import { _useStyleTokens as useStyleTokens_antd_5_hostless } from "../antd_5_hostless/PlasmicStyleTokensProvider"; // plasmic-import: ohDidvG9XsCeFumugENU3J/styleTokensProvider
import { _useStyleTokens as useStyleTokens_plasmic_rich_components } from "../plasmic_rich_components/PlasmicStyleTokensProvider"; // plasmic-import: jkU633o1Cz7HrJdwdxhVHk/styleTokensProvider
import "@plasmicapp/react-web/lib/plasmic.css";
import projectcss from "./plasmic.module.css"; // plasmic-import: 34tvEQuyqfK98iGCjMbawB/projectcss
import sty from "./PlasmicBlockStaffListPersonWithImages.module.css"; // plasmic-import: 1R3VLUIpzgA9/css

createPlasmicElementProxy;

export const PlasmicBlockStaffListPersonWithImages__VariantProps = new Array();

export const PlasmicBlockStaffListPersonWithImages__ArgProps = new Array();

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicBlockStaffListPersonWithImages__RenderFunc(props) {
  const { variants, overrides, forNode } = props;
  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );
  const $props = {
    ...args,
    ...variants
  };
  const __nextRouter = useNextRouter();
  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;
  const stateSpecs = React.useMemo(
    () => [
      {
        path: "variable",
        type: "private",
        variableType: "text",
        initFunc: ({ $props, $state, $queries, $ctx }) =>
          (() => {
            try {
              return (() => {
                return (() => {
                  const resp = $queries.getContent?.data?.response;
                  if (!resp) return [];
                  const assets = resp?.includes?.Asset ?? [];
                  const assetMap = Object.fromEntries(
                    assets.map(a => [a?.sys?.id, a]).filter(([k, v]) => k && v)
                  );
                  const authors = [];
                  const seen = new Set();
                  const lower = s =>
                    typeof s === "string" ? s.toLowerCase() : "";
                  const isAuthor = n =>
                    lower(n?.sys?.contentType?.sys?.id) === "author" ||
                    lower(n?.sys?.contentTypeId) === "author" ||
                    lower(n?.__typename) === "author";
                  const collect = node => {
                    if (!node) return;
                    if (Array.isArray(node)) {
                      node.forEach(collect);
                      return;
                    }
                    if (typeof node !== "object") return;
                    const id = node?.sys?.id;
                    if (id && isAuthor(node) && !seen.has(id)) {
                      seen.add(id);
                      authors.push(node);
                    }
                    for (const k in node) {
                      const v = node[k];
                      if (v && typeof v === "object") collect(v);
                    }
                  };
                  collect(resp);
                  const toRow = entry => {
                    const f = entry?.fields ?? entry ?? {};
                    let photo = "";
                    const photoId = f?.photo?.sys?.id;
                    const graphUrl = f?.photo?.url;
                    const strUrl = typeof f?.photo === "string" ? f.photo : "";
                    if (photoId) {
                      const url = assetMap[photoId]?.fields?.file?.url;
                      photo = url
                        ? String(url).startsWith("//")
                          ? "https:" + url
                          : url
                        : "";
                    } else if (graphUrl) {
                      photo = graphUrl;
                    } else if (strUrl) {
                      photo = strUrl;
                    }
                    const orderNum = Number(f?.order);
                    const order = Number.isFinite(orderNum) ? orderNum : 0;
                    const fullName =
                      f?.fullName ||
                      f?.name ||
                      f?.councilTitle ||
                      f?.title ||
                      "";
                    return {
                      id: entry?.sys?.id || "",
                      order,
                      fullName,
                      councilTitle: f?.councilTitle || "",
                      photo,
                      councilBio: f?.councilBio || f?.bio || ""
                    };
                  };
                  return authors
                    .map(toRow)
                    .sort(
                      (a, b) =>
                        a.order - b.order ||
                        (a.fullName || "").localeCompare(b.fullName || "")
                    );
                })();
              })();
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return undefined;
              }
              throw e;
            }
          })()
      }
    ],

    [$props, $ctx, $refs]
  );
  const $state = useDollarState(stateSpecs, {
    $props,
    $ctx,
    $queries: {},
    $refs
  });
  const styleTokensClassNames = _useStyleTokens();
  const styleTokensClassNames_antd_5_hostless =
    useStyleTokens_antd_5_hostless();
  const styleTokensClassNames_plasmic_rich_components =
    useStyleTokens_plasmic_rich_components();
  return (
    <div
      data-plasmic-name={"root"}
      data-plasmic-override={overrides.root}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        projectcss.all,
        projectcss.root_reset,
        projectcss.plasmic_default_styles,
        projectcss.plasmic_mixins,
        styleTokensClassNames,
        styleTokensClassNames_antd_5_hostless,
        styleTokensClassNames_plasmic_rich_components,
        sty.root
      )}
    />
  );
}

const PlasmicDescendants = {
  root: ["root"]
};

function makeNodeComponent(nodeName) {
  const func = function (props) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicBlockStaffListPersonWithImages__ArgProps,
          internalVariantPropNames:
            PlasmicBlockStaffListPersonWithImages__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicBlockStaffListPersonWithImages__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "root") {
    func.displayName = "PlasmicBlockStaffListPersonWithImages";
  } else {
    func.displayName = `PlasmicBlockStaffListPersonWithImages.${nodeName}`;
  }
  return func;
}

export const PlasmicBlockStaffListPersonWithImages = Object.assign(
  // Top-level PlasmicBlockStaffListPersonWithImages renders the root element
  makeNodeComponent("root"),
  {
    // Helper components rendering sub-elements
    // Metadata about props expected for PlasmicBlockStaffListPersonWithImages
    internalVariantProps: PlasmicBlockStaffListPersonWithImages__VariantProps,
    internalArgProps: PlasmicBlockStaffListPersonWithImages__ArgProps
  }
);

export default PlasmicBlockStaffListPersonWithImages;
/* prettier-ignore-end */
